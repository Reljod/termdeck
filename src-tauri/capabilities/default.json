{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the TermDeck window needs",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
